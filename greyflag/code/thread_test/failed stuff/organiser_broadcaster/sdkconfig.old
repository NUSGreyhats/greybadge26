# Raw 802.15.4 only — no OpenThread stack
CONFIG_IEEE802154_ENABLED=y
CONFIG_OPENTHREAD_ENABLED=n

# mbedTLS CCM cipher (required for AES-128-CCM*)
CONFIG_MBEDTLS_CCM_C=y

CONFIG_LOG_DEFAULT_LEVEL_INFO=y
